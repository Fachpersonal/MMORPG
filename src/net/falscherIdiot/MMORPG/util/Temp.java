package net.falscherIdiot.MMORPG.util;

// import java.io.BufferedReader;
// import java.io.File;
// import java.io.FileReader;
// import java.util.HashMap;

// import org.json.*;

public class Temp {

    public static void main(String[] args) {
        // HashMap<String, JSONObject> items = new HashMap<>();
        // HashMap<String, JSONObject> utils = new HashMap<>();
        // // HashMap<String, JSONObject> trans = new HashMap<>();
        // try {
        // String str = "";
        // String[] dirs = new File("./gen/").list();

        // for (int i = 0; i < dirs.length; i++) {
        // String[] files = new File("./gen/" + dirs[i]).list();
        // for (int j = 0; j < files.length; j++) {
        // BufferedReader br = new BufferedReader(
        // new FileReader(new File("./gen/" + dirs[i] + "/" + files[j])));
        // String tmp;
        // while ((tmp = br.readLine()) != null) {
        // str += tmp;
        // }
        // System.out.println(str);
        // br.close();
        // if (dirs[i].equalsIgnoreCase("items")) {
        // if (files[j].equalsIgnoreCase("primary_weapons.json")) {
        // items.put("primary", new JSONObject(str));
        // } else if (files[j].equalsIgnoreCase("special_weapons.json")) {
        // items.put("special", new JSONObject(str));
        // } else if (files[j].equalsIgnoreCase("destructive_weapons.json")) {
        // items.put("destructive", new JSONObject(str));
        // }
        // }
        // // } else if (dirs[i].equalsIgnoreCase("util")) {
        // // if (files[j].equalsIgnoreCase("maps.json")) {
        // // utils.put("maps", new JSONObject(str));
        // // } else if (files[j].equalsIgnoreCase("screens.json")) {
        // // utils.put("screens", new JSONObject(str));
        // // }
        // // } else if (dirs[i].equalsIgnoreCase("trans")) {
        // // // trans.put(files[j].split(".")[0], new JSONObject(str));
        // // }
        // }
        // }
        // JSONObject x = items.get("primary");
        // System.out.println(x.toString(4));

        // // items.put("primary_weapons", new JSONObject(str));
        // // System.out.println(items.get("primary_weapons").toString(4));

        // // BufferedReader br = new BufferedReader(new FileReader(new
        // // File("test.json")));
        // // BufferedWriter bw = new BufferedWriter(new FileWriter(new
        // // File("test.json")));

        // // JSONStringer jw = new JSONStringer();
        // // jw.object();
        // // jw.key("test").value("OMG this works!");
        // // jw.key("arr");
        // //
        // jw.object().key("name").value("Peter").key("address").value(68644).endObject();
        // // jw.endObject();
        // // String str = jw.toString();
        // // JSONObject jo = new JSONObject(str);
        // // bw.write(jo.toString(4));
        // // bw.close();

        // // String x = "";
        // // String s;
        // // while ((s = br.readLine()) != null) {
        // // x += s;
        // // }

        // // System.out.println(new JSONObject(x).toString(4));
        // // br.close();
        // } catch (Exception e) {
        // e.printStackTrace();
        // }
    }

}
